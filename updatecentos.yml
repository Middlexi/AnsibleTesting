---
 - hosts: 192.168.6.131
   tasks:
      - name: install all updates
            yum:
            name: '*'
            update_cache: yes
            state: latest
            register: result
      - name: Show Output
            debug: msg="{{ result.stdout_lines }}"